
/** Used for describing when a configuration change behavior should be invoked. */
export enum BehaviorTiming {
    Delayed,
    Immediate
}

/** Describes if configuration change notifications are to be used */
export enum NotificationMode {
    On,
    Silent
}